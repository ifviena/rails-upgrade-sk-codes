<%= form_with(model: skeleton) do |form| %>
  <% if skeleton.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(skeleton.errors.count, "error") %> prohibited this skeleton from being saved:</h2>

      <ul>
        <% skeleton.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :facts %>
    <%= form.text_area :facts %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
